'use client';import { useSearchParams,useRouter } from 'next/navigation';import { useState } from 'react';import { PageShell } from '@/components/page-shell';
export default function VerifyEmailPage(){const p=useSearchParams();const r=useRouter();const email=p.get('email')||'';const [code,setCode]=useState('');const [msg,setMsg]=useState('');const [err,setErr]=useState('');return <PageShell><div className='mx-auto max-w-md p-6'><h1 className='text-2xl mb-4'>Verify email</h1><form className='space-y-3' onSubmit={async(e)=>{e.preventDefault();setErr('');const res=await fetch('/api/auth/verify-email',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,code})});if(!res.ok){setErr('Invalid or expired code.');return;}r.push('/dashboard');}}><input className='w-full border p-2' value={code} onChange={e=>setCode(e.target.value)} placeholder='6-digit code'/>{err&&<p className='text-red-600'>{err}</p>}<button className='bg-black text-white px-4 py-2 rounded w-full'>Verify</button></form><button className='mt-3 underline' onClick={async()=>{const res=await fetch('/api/auth/resend-verification',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});setMsg(res.ok?'Code resent.':'Please wait before requesting another code.')}}>Resend code</button>{msg&&<p>{msg}</p>}</div></PageShell>}
