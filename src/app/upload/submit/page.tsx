import { PageShell } from '@/components/page-shell'
import UploadSubmitClient from './UploadSubmitClient'

export default function UploadSubmitPage() {
  return (
    <PageShell>
      <UploadSubmitClient />
    </PageShell>
  )
}