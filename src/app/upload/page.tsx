import { PageShell } from '@/components/page-shell'
import UploadAgreementClient from './UploadAgreementClient'

export default function UploadAgreementPage() {
  return (
    <PageShell>
      <UploadAgreementClient />
    </PageShell>
  )
}