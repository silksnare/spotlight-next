'use client';import {useState} from 'react';import {PageShell} from '@/components/page-shell';
export default function ForgotPasswordPage(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');return <PageShell><div className='mx-auto max-w-md p-6'><h1 className='text-2xl mb-4'>Forgot password</h1><form className='space-y-3' onSubmit={async(e)=>{e.preventDefault();await fetch('/api/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});setMsg('If the account exists, a reset code has been sent.');}}><input className='w-full border p-2' value={email} onChange={e=>setEmail(e.target.value)} placeholder='Email'/><button className='bg-black text-white px-4 py-2 rounded w-full'>Send reset code</button></form>{msg&&<p className='mt-2'>{msg}</p>}</div></PageShell>}
