'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { PageShell } from '@/components/page-shell';
export default function RegisterPage(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');return <PageShell><div className='mx-auto max-w-md p-6'><h1 className='text-2xl mb-4'>Register</h1><form className='space-y-3' onSubmit={async(e)=>{e.preventDefault();setError('');const res=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(!res.ok){setError('Unable to register.');return;}r.push(`/verify-email?email=${encodeURIComponent(email)}`);}}><input className='w-full border p-2' placeholder='Email' value={email} onChange={e=>setEmail(e.target.value)}/><input className='w-full border p-2' type='password' placeholder='Password' value={password} onChange={e=>setPassword(e.target.value)}/><p className='text-sm text-gray-500'>Minimum 8 chars, uppercase, lowercase, and number.</p>{error&&<p className='text-red-600'>{error}</p>}<button className='bg-black text-white px-4 py-2 rounded w-full'>Create account</button></form></div></PageShell>}
